# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CoffePlace' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CoffePlace
  pod 'RxSwift'
  pod 'RealmSwift'
  pod 'RxRealm'
  pod 'RxCocoa'

  target 'CoffePlaceTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CoffePlaceUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
